using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class mazeGen1 : MonoBehaviour
{/*
    int cell = 0;
    int wall = 1;
    int unvisited = -1;
    int height = 11;
    int width = 11;

    private float[,] init_maze()
    {
        float[,] maze;
        for (int x = 0; x < width; x++)
        {
            for (int y = 0; y < width; y++)
            {
                maze[x, y] = unvisited;
            }
        }
        return maze;
    }

    public float[,] generateMaze()
    {
        float[,] maze;
        maze = init_maze();

        return maze;
    }
    */
}